def factorial(numero):
    """
    Este código es una función que recibe como parametro un número
    y devuelve el factorial del número pasado por parámetro.
    """
    factorial = 1
    for numero in range(1, numero + 1):
        factorial = factorial * numero
    return factorial
    """
    print("El factorial de un número es: " + str(factorial))
    """

factorial(5)

def factorial2(numero):
    """
    Este código es una función que recibe como parametro un número
    y devuelve el factorial del número pasado por parámetro.
    """
    factorial = 1
    for numero in range(1, numero + 1):
        factorial = factorial * numero
    return factorial
    """
    print("El factorial de un número es: " +